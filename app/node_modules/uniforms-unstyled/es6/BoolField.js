import { __rest } from "tslib";
import React from 'react';
import { connectField, filterDOMProps } from 'uniforms';
const Bool = (_a) => {
    var { disabled, id, inputRef, label, name, onChange, value } = _a, props = __rest(_a, ["disabled", "id", "inputRef", "label", "name", "onChange", "value"]);
    return (React.createElement("div", Object.assign({}, filterDOMProps(props)),
        React.createElement("input", { checked: value, disabled: disabled, id: id, name: name, onChange: () => disabled || onChange(!value), ref: inputRef, type: "checkbox" }),
        label && React.createElement("label", { htmlFor: id }, label)));
};
export default connectField(Bool);
