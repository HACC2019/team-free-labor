import { __rest } from "tslib";
import React from 'react';
import cloneDeep from 'lodash/cloneDeep';
import { connectField, filterDOMProps } from 'uniforms';
const ListAdd = (_a) => {
    var { disabled, parent, value } = _a, props = __rest(_a, ["disabled", "parent", "value"]);
    const limitNotReached = !disabled && !(parent.maxCount <= value.length);
    return (React.createElement("span", Object.assign({}, filterDOMProps(props), { onClick: () => limitNotReached &&
            parent.onChange(parent.value.concat([cloneDeep(value)])) }), "+"));
};
export default connectField(ListAdd, {
    includeParent: true,
    initialValue: false,
});
