import { __rest } from "tslib";
import React from 'react';
import { BaseField, filterDOMProps, nothing } from 'uniforms';
const ErrorsField = (_a, { uniforms: { error, schema } }) => {
    var { children } = _a, props = __rest(_a, ["children"]);
    return !error && !children ? (nothing) : (React.createElement("div", Object.assign({}, filterDOMProps(props)),
        children,
        React.createElement("ul", null, schema.getErrorMessages(error).map((message, index) => (React.createElement("li", { key: index }, message))))));
};
ErrorsField.contextTypes = BaseField.contextTypes;
export default ErrorsField;
