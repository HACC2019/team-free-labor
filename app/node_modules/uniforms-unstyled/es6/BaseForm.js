import { BaseForm } from 'uniforms';
const Unstyled = (parent) => { var _a; return _a = class extends parent {
    },
    _a.Unstyled = Unstyled,
    _a.displayName = `Unstyled${parent.displayName}`,
    _a; };
export default Unstyled(BaseForm);
